apiVersion: v1
kind: Service
metadata:
  name: {{ include "caas-docs.fullname" . }}
  labels:
    {{- include "caas-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "caas-docs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

